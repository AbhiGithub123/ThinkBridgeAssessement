import axios from "../helpers/axios";

export const addProduct= (form)=>{
    return async dispatch=>{
        //We call backend api here 
       // const res=await axios.post('/product/create',form);
        
    }
}